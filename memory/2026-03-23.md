
## 2026-03-23 16:32 UTC — Workflow & Changelog Automation

### GitHub Actions CI/CD Pipeline Updates
- **ChangelogModal Popup fix**: Implemented proper overlay with backdrop-blur, body scroll lock, swipe-to-close, and h-[85vh] height limit (Commit bb67692)
- **Automatic changelog generation**: Created Python script (`generate-changelog.py`) that parses conventional commits since last version tag and generates changelog entries in the correct format
- **Workflow enhancements**: 
  - Removed artifact upload (no APK build on GitHub, only Play Console deploy)
  - Added automatic version tagging (`v6.3.4`, etc.) after successful deploy
  - Integrated changelog generation as part of the deploy workflow
- **Changelog format**: Script maps commit types to icons/sections:
  - `feat:` → Sparkles + "Neue Features" + "Neu:" prefix
  - `fix:` → Bug + "Bugfixes" + "Fix:" prefix  
  - `refactor:`/`chore:` → Zap + "Code-Qualität" + "Refactor:"/"Chore:" prefix
  - `docs:` → FileText + "Dokumentation" + "Docs:" prefix
  - `perf:` → Rocket + "Performance" + "Perf:" prefix

### Current Status
- Workflow ready for next push to `main`
- Changelog generation tested locally (works but quality depends on commit messages)
- Version 6.3.3 deployed to Play Console Internal Testing (versionCode 68)
- All 3 Quick Wins live: Backup button, last backup display, GDrive offline warning

### Key Decisions
1. **No GitHub Releases** - Only Play Console deployments
2. **Automatic changelog from commits** - Requires clean conventional commit messages
3. **Version tagging** - Creates `vX.Y.Z` tags for future changelog generation
4. **Commit quality matters** - Changelog entries reflect commit message quality

### Next Actions
- Push current changes to trigger workflow with new automation
- Consider improving script to filter out routine `chore: bump` commits
- Maintain clean conventional commit messages for better changelog quality

